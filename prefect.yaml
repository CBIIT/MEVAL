# Welcome to your prefect.yaml file! You can use this file for storing and managing
# configuration for deploying your flows. We recommend committing this file to source
# control along with your flow code.

# Generic metadata about this project
name: s3-Prefect-Workflow
prefect-version: 3.14.19

# build section allows you to manage and build docker images
build: null

# push section allows you to manage if and how this project is uploaded to remote locations
push: null

# the deployments section allows you to provide configuration for deploying flows
deployments:
  - name: meval-dataloader
    version: null
    tags: ["MEVAL","dataloader"]
    description: |
      MEVAL upsert workflow deployment. This workflow will UPSERT data, meaning it only creates if new and update if existing. Existing relationships in DB won't be touched. No deletion is performed.
    entrypoint: workflows/prefect/upsert_workflow.py:upsert_files
    parameters:
      uuid_field: "guid"
      delimiter: ";"
    pull:
      - prefect.deployments.steps.git_clone:
          id: main_repo_clone_step
          repository: https://github.com/CBIIT/MEVAL.git
          branch: main
          include_submodules: True
      - prefect.deployments.steps.pip_install_requirements:
          requirements_file: requirements_python3.13.txt
          directory: "{{ main_repo_clone_step.directory }}"
          stream_output: True    
    # infra-specific fields
    work_pool:
      name: ccdi-dcc-16gb-prefect-3.4.19-python3.13
      work_queue_name: null
      job_variables: {}

  - name: meval-wipe-db
    version: null
    tags: ["MEVAL","dataloader"]
    description: "Prefect test deployment for wiping out database workflow"
    entrypoint: workflows/prefect/wipe_database.py:wipe_database
    pull:
      - prefect.deployments.steps.git_clone:
          id: main_repo_clone_step
          repository: https://github.com/CBIIT/MEVAL.git
          branch: 1.0.0
          include_submodules: True
      - prefect.deployments.steps.pip_install_requirements:
          requirements_file: requirements_python3.13.txt
          directory: "{{ main_repo_clone_step.directory }}"
          stream_output: True    
    # infra-specific fields
    work_pool:
      name: ccdi-dcc-16gb-prefect-3.4.19-python3.13
      work_queue_name: null
      job_variables: {}

  - name: meval-delete-study
    version: null
    tags: ["MEVAL","dataloader"]
    description: "Prefect test deployment for deleting subgraph in the database workflow"
    entrypoint: workflows/prefect/delete_database_study_subgraph.py:delete_database_study_subgraph
    parameters:
      root_node_label: "study"
      root_node_property: "study_id"
    pull:
      - prefect.deployments.steps.git_clone:
          id: main_repo_clone_step
          repository: https://github.com/CBIIT/MEVAL.git
          branch: 1.0.0
          include_submodules: True
      - prefect.deployments.steps.pip_install_requirements:
          requirements_file: requirements_python3.13.txt
          directory: "{{ main_repo_clone_step.directory }}"
          stream_output: True    
    # infra-specific fields
    work_pool:
      name: ccdi-dcc-16gb-prefect-3.4.19-python3.13
      work_queue_name: null
      job_variables: {}

  - name: meval-anchored-traversals-viz
    version: null
    tags: ["MEVAL","dataloader"]
    description: "Prefect test deployment for visualizing anchored traversals workflow"
    entrypoint: workflows/prefect/find_anchored_traversals.py:find_anchored_traversals
    parameters:
      root_node_label: "study"
      root_node_property: "study_id"
      output_bucket: "ccdi-validation"
    pull:
      - prefect.deployments.steps.git_clone:
          id: main_repo_clone_step
          repository: https://github.com/CBIIT/MEVAL.git
          branch: 1.0.0
          include_submodules: True
      - prefect.deployments.steps.pip_install_requirements:
          requirements_file: requirements_python3.13.txt
          directory: "{{ main_repo_clone_step.directory }}"
          stream_output: True    
    # infra-specific fields
    work_pool:
      name: ccdi-dcc-16gb-prefect-3.4.19-python3.13
      work_queue_name: null
      job_variables: {}

  - name: meval-validation
    version: null
    tags: ["MEVAL", "dataloader", "validation"]
    description: |
      Validates a set of TSV files for a study or a program using the Validator class. Please include all the files of a study/program when running the validation pipeline as validating partial files may have data issues overlooked.
      Validation pipeline includes: tsv format check, record check, relationship check, and unique key check.
      NOTE: "tsv_format_check" is mandatory as other validation checks depend on the files passing the format check.
    entrypoint: workflows/prefect/validate_tsv_files.py:validate_tsv_files
    parameters:
      delimiter: ";"
    pull:
      - prefect.deployments.steps.git_clone:
          id: main_repo_clone_step
          repository: https://github.com/CBIIT/MEVAL.git
          branch: 1.0.0
          include_submodules: True
      - prefect.deployments.steps.pip_install_requirements:
          requirements_file: requirements_python3.13.txt
          directory: "{{ main_repo_clone_step.directory }}"
          stream_output: True    
    # infra-specific fields
    work_pool:
      name: ccdi-dcc-16gb-prefect-3.4.19-python3.13
      work_queue_name: null
      job_variables: {}

  - name: meval-uuid-creation
    version: null
    tags: ["MEVAL","dataloader", "uuid"]
    description: "Prefect test deployment for adding uuid to a set of tsv files for a study or a program"
    entrypoint: workflows/prefect/add_uuid_to_files.py:add_uuid_to_files
    parameters:
      uuid_col_name: "guid"
      delimiter: ";"
    pull:
      - prefect.deployments.steps.git_clone:
          id: main_repo_clone_step
          repository: https://github.com/CBIIT/MEVAL.git
          branch: 1.0.0
          include_submodules: True
      - prefect.deployments.steps.pip_install_requirements:
          requirements_file: requirements_python3.13.txt
          directory: "{{ main_repo_clone_step.directory }}"
          stream_output: True  
    # infra-specific fields
    work_pool:
      name: ccdi-dcc-16gb-prefect-3.4.19-python3.13
      work_queue_name: null
      job_variables: {}

  - name: meval-find-orphan-nodes
    version: null
    tags: ["MEVAL", "dataloader"]
    description: |
      Find any data nodes in the graph database that do not have a path to a root node (e.g. study or program node).
    entrypoint: workflows/prefect/find_db_floating_nodes.py:find_floating_db_nodes
    parameters:
      root_node_label: "study"      
    pull:
      - prefect.deployments.steps.git_clone:
          id: main_repo_clone_step
          repository: https://github.com/CBIIT/MEVAL.git
          branch: main
          include_submodules: True
      - prefect.deployments.steps.pip_install_requirements:
          requirements_file: requirements_python3.13.txt
          directory: "{{ main_repo_clone_step.directory }}"
          stream_output: True    
    # infra-specific fields
    work_pool:
      name: ccdi-dcc-16gb-prefect-3.4.19-python3.13
      work_queue_name: null
      job_variables: {}

  - name: meval-precision-deletion-nodes
    version: null
    tags: ["MEVAL","dataloader"]
    description: "Prefect test deployment for deleting nodes by a provided list of uuid/guid."
    entrypoint: workflows/prefect/precision_deletion_guid.py:precision_deletion_guid
    pull:
      - prefect.deployments.steps.git_clone:
          id: main_repo_clone_step
          repository: https://github.com/CBIIT/MEVAL.git
          branch: main
          include_submodules: True
      - prefect.deployments.steps.pip_install_requirements:
          requirements_file: requirements_python3.13.txt
          directory: "{{ main_repo_clone_step.directory }}"
          stream_output: True    
    # infra-specific fields
    work_pool:
      name: ccdi-dcc-16gb-prefect-3.4.19-python3.13
      work_queue_name: null
      job_variables: {}
  
  - name: meval-validate-submission-against-db
    version: null
    tags: ["MEVAL","dataloader", "validation"]
    description: "Prefect test deployment for validating a submission against the database"
    entrypoint: workflows/prefect/validate_submission_against_db.py:validate_submission_against_db
    pull:
      - prefect.deployments.steps.git_clone:
          id: main_repo_clone_step
          repository: https://github.com/CBIIT/MEVAL.git
          branch: main
          include_submodules: True
      - prefect.deployments.steps.pip_install_requirements:
          requirements_file: requirements_python3.13.txt
          directory: "{{ main_repo_clone_step.directory }}"
          stream_output: True    
    # infra-specific fields
    work_pool:
      name: ccdi-dcc-16gb-prefect-3.4.19-python3.13
      work_queue_name: null
      job_variables: {}